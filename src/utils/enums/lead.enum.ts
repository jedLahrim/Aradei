export enum CompanyType {
  COMPANY = 1,
  INDIVIDUAL = 2,
}

export enum LeadSource {
  FORM = 'FORM',
  INTERNAL_FORM = 'INTERNAL_FORM',
  PORTAL = 'PORTAL',
}

export enum LeadStatus {
  PENDING = 'Pending',
  VALIDATED = 'Validated',
}
